package Indexer; 

import org.codehaus.jackson.JsonParser; 

public class Indexer {
	
	public static void createIndex(String crawledPagesLocation){
		
		//takes as parameter the location of the crawled dump provided by TA 
		//put the location of crawled data somewhere other the location of 
		//of code that you will push to repository since its prob not a good idea to 
		//push a file that large to github  
		
	}

}
